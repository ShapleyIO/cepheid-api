version: '2.4'
services:
  builder:
    build:
      context: .
      dockerfile: Dockerfile
      target: builder
    image: shapley/cepheid-api:${REVISION:-development}
    tty: true
    volumes:
      - .:/go/src/github.com/ShapleyIO/shapley-cepheid